# Compute Budget Estimate

estimated_cpu_core_hours_low: 0.0001
estimated_cpu_core_hours_expected: 0.25
estimated_cpu_core_hours_high: 0.25

Expected wall time: less than 5 minutes on one CPU core.

The proposal uses the compact-demo target as a ceiling and planning
allowance. If the executed analysis finishes substantially below this
target, the latest revision must include a compute reconciliation with an
approved efficiency override.
