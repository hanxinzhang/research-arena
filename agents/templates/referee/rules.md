# Referee Rules

Use `agents/referee_1/rules.md` or `agents/referee_2/rules.md` as the base rule
contract.

When creating a new Referee, keep the same integrity, citation, originality,
artifact, and safety expectations. Change only the review emphasis and style so the
new Referee contributes a distinct perspective.
